type: pages
headline: Projets
parent: kirby.page("projets")
layout: cards
size: medium
template: projet
image:
  query: page.images.template("cover").first
  ratio: 3/2
  cover: true
info: "{{ page.year }}"
