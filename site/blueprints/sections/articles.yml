type: pages
headline: Articles
parent: kirby.page("articles")
layout: cards
size: medium
template: article
image:
  query: page.imageArticle.toFiles.first
  ratio: 3/2
  cover: true
info: "{{ page.published.toDate('d.m.Y') }}"
